const n = prompt('Введите N')
var nn=n;
nn++;
if ((n % 2 === 0) && (typeof nn == 'number')) {
  alert('Число ' + n + ' четное!')
} else if ((n % 2 != 0) && (typeof nn == 'number')) {
  alert('Число ' + n + ' нечетное!')
} else {
  alert('упс, кажется, вы ошиблись:/')
}

-----
Задание выполнено почти верно, но если ввести строку, выходит, что число нечетное, хотя в таком случае должен быть третий вариант - 'упс, кажется, вы ошиблись:/'. Это происходит из-за того, что помимо проверки typeof == 'number' нужно добавить проверку на значение NaN (not a number). Оно получается при попытке привести к числовому формату строку, которая состоит только из букв. typeof NaN возвращает 'number', даже несмотря на то, что NaN не является числом, поэтому для проверки на NaN существует специальная функция isNaN. Прикладываю также верный вариант решения:

const inputValue = prompt('Введите число');

const value = +inputValue;

if(typeof value == "number" && !isNaN(value)){
  if(value == 0) {
    console.log('ноль');
  } else if (value % 2 == 0){
    console.log('чётное');
  } else {
    console.log('нечётное');
  }
} else {
  console.log('Упс, кажется, вы ошиблись');
}
